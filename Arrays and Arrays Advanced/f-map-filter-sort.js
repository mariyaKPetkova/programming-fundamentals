function solve(arr) {
    // function doubleNum (num){
    //     return num * 2;
    // }
    // let array = arr.map(doubleNum);
    // return array

    // за еднократно умножение:
    //   let array = arr.map(x => x * 2)
    //   return array.join(' ')
    // }
// console.log(solve([1, 3]));

//     let arrAsStr = ['1', '3', '5'];
//     let arrAsNum = arrAsStr.map(Number);
//     return arrAsNum.map(x => x - 2)
// }
// console.log(solve())


    // let arrAsNum = [1,2,3];
    // let arrAsStr = arrAsNum.map(x => x.toString());
    // console.log(arrAsStr);


//     function isNegative(num) {
//         return num < 0;
//     }
//     function isPositive (numb){
//         return numb > 0;
//     }
//     function isNull (n){
//         return n === 0
//     }    
//     let fNegative = arr.filter(isNegative).join(' ') //true or false;
//     let fPositive = arr.filter(isPositive).join(' ')
//     let fNull = arr.filter(isNull).join(' ')
//     return `${fNegative} \n ${fPositive} \n ${fNull}`


//     // let fNegative = arr.filter(x => x < 0)
//     // return fNegative
// }
// console.log(solve([1, -3, 2, 0, -9]));


// let words = ['spray', 'limit', 'elite', 'exuberant', 'destruction', 'present'];
// let result = words.filter(word => word.length > 6);
// console.log(result);


// let numbers = [4, 2, 5, 1, 3,10];
// // //one execution:
// console.log(numbers.sort((a, b) => a - b));
// console.log(numbers.sort((a, b) => b - a));
// console.log(numbers.sort());


// function numbAcending (numb){
//     return numb.sort((a, b) => a - b)
// }
// function numDescending (num){
//     return num.sort((a, b) => b - a)
// }
// return `${numDescending(numbers)} \n${numbAcending(numbers)}`
// }
// console.log(solve());

// let words = ['and','Double','big','cake'];
// console.log(words.sort());
// console.log(words.sort((a,b)=>a.localeCompare(b)));
['Ann,22','Bob,32']
let sorted = name.sort((a,b) => a.name.localeCompare(b.hero) || a.age - b.age)
}
solve()